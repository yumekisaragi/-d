module.exports = {
  config: {
    name: "murgi",
    version: "1.0.0",
    author: "Mahiru",
    countDown: 5,
    role: 2,
    category: "fun",
    guide: {
      en: "{pn} @mention"
    }
  },

  onStart: async function ({ api, event }) {
    const mention = Object.keys(event.mentions)[0];

    if (!mention) return api.sendMessage(
      "🫂 যাকে চুদবি তাকে @mention কর",
      event.threadID,
      event.messageID
    );

    const name = event.mentions[mention];
    const tag = [{ id: mention, tag: name }];

    function send(txt, delay = 0) {
      setTimeout(() => {
        api.sendMessage(
          typeof txt === "string" ? txt : txt,
          event.threadID
        );
      }, delay);
    }

    send("তোর সজীব বাপ এর চোদা লো", 0);
    send({ body: `খাংকির পোলা তর মারে চুদি 🥰 ${name}`, mentions: tag }, 3000);
    send({ body: `খাংকির পোলা তর কচি বোন রে আমি সজীব চুদি 😍 ${name}`, mentions: tag }, 5000);
    send({ body: `মাদারচোদ তর আম্মু পম পম খাংকির পো আমি তোর বাপ সজীব 🐰 ${name}`, mentions: tag }, 7000);
    send({ body: `তর কচি ভুদায় কামর দিমু আমি তোর আব্বু সজীব 💔 ${name}`, mentions: tag }, 9000);
    send({ body: `খাংকি মাগির পোলা কথা কম তর আম্মু রে চুদে বানামু বোম ${name}`, mentions: tag }, 12000);
    send({ body: `depression থেকেও তর মাইরে চু*** দি 🤬 ${name}`, mentions: tag }, 15000);
    send({ body: `তর আম্মু রে আচার এর লোভ দেখি চুদি 🤬 ${name}`, mentions: tag }, 17000);
    send({ body: `তর কচি বোনের ভুদা থুতু দিয়ে বানামি 🤟 ${name}`, mentions: tag }, 20000);
    send({ body: `তর দুলাভাই এর কান্দে ফেলে চুদা দিমু 🤝 ${name}`, mentions: tag }, 23000);
    send({ body: `উফফফ তর আম্মুর কালা ভুদায় আমার মাল আউট 😭 ${name}`, mentions: tag }, 25000);
    send({ body: `অনলাইনের গালিবাজ তুই সজীব এর জারজ মাগির পোলা 😘 ${name}`, mentions: tag }, 28500);
    send({ body: `তর বোনের জামা খুলে দে ✋ ${name}`, mentions: tag }, 31000);
    send({ body: `হাই মাদারচোদ তর আম্মু রে আদর করে চুদি ${name}`, mentions: tag }, 36000);
    send("~ চুদা কি আরো খাবি মাগির পোল 🤖", 39000);
  }
};
